[project]
name = "teamcity-ci-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "tanvi-bhosale-04", email = "suvarnabhosale65@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
]

[project.scripts]
teamcity-ci-demo = "teamcity_ci_demo:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
